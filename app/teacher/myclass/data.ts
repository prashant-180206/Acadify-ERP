export const class_attendance = {
  class: "CS-K",
  total_students: 80,
  present_students: 80,
  attendance_percentage: 80,
};

export const student_queries: { pending: string[] } = {
  pending: [], // Array of query strings, empty if no pending queries
};
